# Production artifact contract (stub)

GHCR source-build contracts, `IMAGE_TAG` invariants, and migrate image ownership for the StreamPulse backend are documented in private **streampulse-ops** and **streampulse-backend**.

This public Streamclone repo builds **core** desktop images only (`metadata`, `video`, `chat`, `emote`, `frontend`, `migrate` for core schema).

See [streampulse-product-boundary.md](streampulse-product-boundary.md).
