# Stage 3 — analytics workers + scraper
services:
  analytics-workers:
    mem_limit: 1280m
    cpus: "1.0"
    pids_limit: 256
    restart: unless-stopped

  scraper:
    mem_limit: 1280m
    pids_limit: 256
    restart: unless-stopped
