# Bronze acceptance smoke overlay — top-100 roster, ~6h operator run.
# Merge with profile-archive.env + .env.local (Azure connection string, OAuth).
TIER0_ROSTER_TOP_N=100
BRONZE_TOP_N=100
GOLD_BACKFILL_ENABLED=false
