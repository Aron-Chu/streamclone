# Copy to deploy/env/helm-local.yaml (gitignored) and override dev defaults.
# Used by: make helm-up (this file is applied automatically when helm-local.yaml is missing)
#
# Default Grafana login with these values: admin / devpulse

influxdb:
  adminToken: "dev-pulse-token"

grafana:
  adminPassword: "devpulse"
